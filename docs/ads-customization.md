# ADS configuration compatibility

ADS is now a legacy configuration alias for the SciX integration. Existing `source.ads`, `ADS_API_TOKEN`, `ads:<bibcode>` state identifiers, and `.runtime/state/ads.json` continue to work.

See [SciX customization and migration](./scix-customization.md) for the current architecture, privacy boundary, operational rules, and migration notes.
